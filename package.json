{
  "name": "tac-apps",
  "private": true,
  "workspaces": [
    "core",
    "apps/*"
  ],
  "scripts": {
    "test": "npm run test --workspace core",
    "tac-log:build": "npm run build --workspace tac-log",
    "tac-log:dist:mac": "npm run dist:mac --workspace tac-log",
    "tac-log:dist:win": "npm run dist:win --workspace tac-log"
  }
}
